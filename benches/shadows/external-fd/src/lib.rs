//! A shadow of `fd.usage.kdl` in usage's vocabulary, generated by
//! `cargo run -p xtask -- gen-shadow`.
//!
//! Do not edit: regenerate it. It exists to be compiled and parsed against, so
//! that the parser can be measured at a real CLI's scale rather than a toy one.
#![allow(dead_code, unused_imports)]

use usage_derive::{Args, Cli, Subcommands, ValueEnum};

/// A program to find entries in your filesystem with regex and glob based matching. By default, fd respects gitignore rules, ignores hidden directories, and is case insensitive.
#[derive(Cli)]
#[usage(
    bin = "fd",
    name = "fd",
    version = "10.4.2",
    unknown_flags = "error",
    args_override_self = false,
    group("execs")
)]
pub struct Cli {
    #[usage(
        help = "Search hidden files and directories",
        long_help = "Include hidden directories and files in the search results (default: hidden files and directories are skipped). Files and directories are considered to be hidden if their name starts with a `.` sign (dot). Any files or directories that are ignored due to the rules described by --no-ignore are still ignored unless otherwise specified. The flag can be overridden with --no-hidden.",
        long = "hidden",
        short = 'H'
    )]
    pub hidden: bool,
    /// Overrides --hidden
    #[usage(long = "no-hidden", hide)]
    pub no_hidden: bool,
    #[usage(
        help = "Do not respect .(git|fd)ignore files",
        long_help = "Show search results from files and directories that would otherwise be ignored by '.gitignore', '.ignore', '.fdignore', or the global ignore file, The flag can be overridden with --ignore.",
        long = "no-ignore",
        short = 'I'
    )]
    pub no_ignore: bool,
    /// Overrides --no-ignore
    #[usage(long = "ignore", hide)]
    pub ignore: bool,
    #[usage(
        help = "Do not respect .gitignore files",
        long_help = "Show search results from files and directories that would otherwise be ignored by '.gitignore' files. The flag can be overridden with --ignore-vcs.",
        long = "no-ignore-vcs"
    )]
    pub no_ignore_vcs: bool,
    /// Overrides --no-ignore-vcs
    #[usage(long = "ignore-vcs", hide)]
    pub ignore_vcs: bool,
    #[usage(
        help = "Do not require a git repository to respect gitignores. By default, fd will only respect global gitignore rules, .gitignore rules, and local exclude rules if fd detects that you are searching inside a git repository. This flag allows you to relax this restriction such that fd will respect all git related ignore rules regardless of whether you're searching in a git repository or not",
        long_help = "Do not require a git repository to respect gitignores. By default, fd will only respect global gitignore rules, .gitignore rules, and local exclude rules if fd detects that you are searching inside a git repository. This flag allows you to relax this restriction such that fd will respect all git related ignore rules regardless of whether you're searching in a git repository or not.\n\nThis flag can be disabled with --require-git.",
        long = "no-require-git"
    )]
    pub no_require_git: bool,
    /// Overrides --no-require-git
    #[usage(long = "require-git", hide)]
    pub require_git: bool,
    #[usage(
        help = "Do not respect .(git|fd)ignore files in parent directories",
        long_help = "Show search results from files and directories that would otherwise be ignored by '.gitignore', '.ignore', or '.fdignore' files in parent directories. The flag can be overridden with --ignore-parent.",
        long = "no-ignore-parent"
    )]
    pub no_ignore_parent: bool,
    /// Overrides --no-ignore-parent
    #[usage(long = "ignore-parent", hide)]
    pub ignore_parent: bool,
    /// Do not respect the global ignore file
    #[usage(long = "no-global-ignore-file", hide)]
    pub no_global_ignore_file: bool,
    #[usage(
        help = "Unrestricted search, alias for '--no-ignore --hidden'",
        long_help = "Perform an unrestricted search, including ignored and hidden files. This is an alias for '--no-ignore --hidden'.",
        long = "unrestricted",
        short = 'u',
        count
    )]
    pub unrestricted: u8,
    #[usage(
        help = "Case-sensitive search (default: smart case)",
        long_help = "Perform a case-sensitive search. By default, fd uses case-insensitive searches, unless the pattern contains an uppercase character (smart case).",
        long = "case-sensitive",
        short = 's'
    )]
    pub case_sensitive: bool,
    #[usage(
        help = "Case-insensitive search (default: smart case)",
        long_help = "Perform a case-insensitive search. By default, fd uses case-insensitive searches, unless the pattern contains an uppercase character (smart case).",
        long = "ignore-case",
        short = 'i'
    )]
    pub ignore_case: bool,
    #[usage(
        help = "Glob-based search (default: regular expression)",
        long_help = "Perform a glob-based search instead of a regular expression search.",
        long = "glob",
        short = 'g',
        conflicts = "--fixed-strings"
    )]
    pub glob: bool,
    #[usage(
        help = "Regular-expression based search (default)",
        long_help = "Perform a regular-expression based search (default). This can be used to override --glob.",
        long = "regex"
    )]
    pub regex: bool,
    #[usage(
        help = "Treat pattern as literal string instead of regex",
        long_help = "Treat the pattern as a literal string instead of a regular expression. Note that this also performs substring comparison. If you want to match on an exact filename, consider using '--glob' or '--exact' instead.",
        long = "fixed-strings",
        alias = "literal",
        short = 'F'
    )]
    pub fixed_strings: bool,
    #[usage(
        help = "Match the entire filename exactly (literal, non-substring)",
        long_help = "Perform an exact match. This is equivalent to '--fixed-strings' but requires the pattern to match the entire filename (or path if '--full-path' is used), rather than a substring. Special regex characters in the pattern are treated as literal characters.",
        long = "exact",
        conflicts("--glob", "--fixed-strings")
    )]
    pub exact: bool,
    #[usage(
        help = "Additional search patterns that need to be matched",
        long_help = "Add additional required search patterns, all of which must be matched. Multiple additional patterns can be specified. The patterns are regular expressions, unless '--glob' or '--fixed-strings' is used.",
        long = "and",
        allow_hyphen_values,
        value_name = "pattern",
        var,
        variadic
    )]
    pub and: ::std::vec::Vec<::std::string::String>,
    #[usage(
        help = "Show absolute instead of relative paths",
        long_help = "Shows the full path starting from the root as opposed to relative paths. The flag can be overridden with --relative-path.",
        long = "absolute-path",
        short = 'a'
    )]
    pub absolute_path: bool,
    /// Overrides --absolute-path
    #[usage(long = "relative-path", hide)]
    pub relative_path: bool,
    #[usage(
        help = "Use a long listing format with file metadata",
        long_help = "Use a detailed listing format like 'ls -l'. This is basically an alias for '--exec-batch ls -l' with some additional 'ls' options. This can be used to see more metadata, to show symlink targets and to achieve a deterministic sort order.",
        long = "list-details",
        short = 'l',
        group = "execs",
        conflicts = "--absolute-path"
    )]
    pub list_details: bool,
    #[usage(
        help = "Follow symbolic links",
        long_help = "By default, fd does not descend into symlinked directories. Using this flag, symbolic links are also traversed. Flag can be overridden with --no-follow.",
        long = "follow",
        alias = "dereference",
        short = 'L'
    )]
    pub follow: bool,
    /// Overrides --follow
    #[usage(long = "no-follow", hide)]
    pub no_follow: bool,
    #[usage(
        help = "Search full abs. path (default: filename only)",
        long_help = "By default, the search pattern is only matched against the filename (or directory name). Using this flag, the pattern is matched against the full (absolute) path. Example:\n  fd --glob -p '**/.git/config'",
        long = "full-path",
        short = 'p'
    )]
    pub full_path: bool,
    #[usage(
        help = "Separate search results by the null character",
        long_help = "Separate search results by the null character (instead of newlines). Useful for piping results to 'xargs'.",
        long = "print0",
        short = '0',
        conflicts = "--list-details"
    )]
    pub print0: bool,
    #[usage(
        help = "Set maximum search depth (default: none)",
        long_help = "Limit the directory traversal to a given depth. By default, there is no limit on the search depth.",
        long = "max-depth",
        alias = "maxdepth",
        short = 'd',
        value_name = "depth"
    )]
    pub max_depth: ::std::option::Option<::std::string::String>,
    #[usage(
        help = "Only show search results starting at the given depth.",
        long_help = "Only show search results starting at the given depth. See also: '--max-depth' and '--exact-depth'",
        long = "min-depth",
        alias = "mindepth",
        value_name = "depth"
    )]
    pub min_depth: ::std::option::Option<::std::string::String>,
    #[usage(
        help = "Only show search results at the exact given depth",
        long_help = "Only show search results at the exact given depth. This is an alias for '--min-depth <depth> --max-depth <depth>'.",
        long = "exact-depth",
        conflicts("--max-depth", "--min-depth"),
        value_name = "depth"
    )]
    pub exact_depth: ::std::option::Option<::std::string::String>,
    #[usage(
        help = "Exclude entries that match the given glob pattern",
        long_help = "Exclude files/directories that match the given glob pattern. This overrides any other ignore logic. Multiple exclude patterns can be specified.\n\nExamples: {n}  --exclude '*.pyc' {n}  --exclude node_modules",
        long = "exclude",
        short = 'E',
        value_name = "glob",
        var,
        variadic
    )]
    pub exclude: ::std::vec::Vec<::std::string::String>,
    #[usage(
        help = "Do not traverse into directories that match the search criteria. If you want to exclude specific directories, use the '--exclude=…' option",
        long_help = "Do not traverse into directories that match the search criteria. If you want to exclude specific directories, use the '--exclude=…' option.",
        long = "prune",
        conflicts("--size", "--exact-depth")
    )]
    pub prune: bool,
    #[usage(
        help = "Filter by type: file (f), directory (d/dir), symlink (l), executable (x), empty (e), socket (s), pipe (p), char-device (c), block-device (b)",
        long_help = "Filter the search by type: {n}  'f' or 'file':         regular files {n}  'd' or 'dir' or 'directory':    directories {n}  'l' or 'symlink':      symbolic links {n}  's' or 'socket':       socket {n}  'p' or 'pipe':         named pipe (FIFO) {n}  'b' or 'block-device': block device {n}  'c' or 'char-device':  character device {n}{n}  'x' or 'executable':   executables {n}  'e' or 'empty':        empty files or directories\n\nThis option can be specified more than once to include multiple file types. Searching for '--type file --type symlink' will show both regular files as well as symlinks. Note that the 'executable' and 'empty' filters work differently: '--type executable' implies '--type file' by default. And '--type empty' searches for empty files and directories, unless either '--type file' or '--type directory' is specified in addition.\n\nExamples: {n}  - Only search for files: {n}      fd --type file … {n}      fd -tf … {n}  - Find both files and symlinks {n}      fd --type file --type symlink … {n}      fd -tf -tl … {n}  - Find executable files: {n}      fd --type executable {n}      fd -tx {n}  - Find empty files: {n}      fd --type empty --type file {n}      fd -te -tf {n}  - Find empty directories: {n}      fd --type empty --type directory {n}      fd -te -td",
        long = "type",
        short = 't',
        hide_possible_values,
        value_name = "filetype",
        value_enum,
        var,
        variadic
    )]
    pub type_: ::std::vec::Vec<TypeValue>,
    #[usage(
        help = "Filter by extension",
        long_help = "Filter results by extension. By default, this matches all entry types (including directories whose names end with the extension). Use `--type` to restrict the search to specific types. Multiple extensions can be specified.\n\nIf you want to search for files without extension, you can use the regex '^[^.]+$' as a normal search pattern.",
        long = "extension",
        short = 'e',
        value_name = "ext",
        var,
        variadic
    )]
    pub extension: ::std::vec::Vec<::std::string::String>,
    #[usage(
        help = "Limit results based on the size of files",
        long_help = "Limit results based on the size of files using the format <+-><NUM><UNIT>.\n   '+': file size must be greater than or equal to this\n   '-': file size must be less than or equal to this\n\nIf neither '+' nor '-' is specified, file size must be exactly equal to this.\n   'NUM':  The numeric size (e.g. 500)\n   'UNIT': The units for NUM. They are not case-sensitive.\nAllowed unit values:\n    'b':  bytes\n    'k':  kilobytes (base ten, 10^3 = 1000 bytes)\n    'm':  megabytes\n    'g':  gigabytes\n    't':  terabytes\n    'ki': kibibytes (base two, 2^10 = 1024 bytes)\n    'mi': mebibytes\n    'gi': gibibytes\n    'ti': tebibytes",
        long = "size",
        short = 'S',
        allow_hyphen_values,
        var,
        variadic
    )]
    pub size: ::std::vec::Vec<::std::string::String>,
    #[usage(
        help = "Filter by file modification time (newer than)",
        long_help = "Filter results based on the file modification time. Files with modification times greater than the argument are returned. The argument can be provided as a specific point in time (YYYY-MM-DD HH:MM:SS or @timestamp) or as a duration (10h, 1d, 35min). If the time is not specified, it defaults to 00:00:00. '--change-newer-than', '--newer', or '--changed-after' can be used as aliases.\n\nExamples: {n}    --changed-within 2weeks {n}    --change-newer-than '2018-10-27 10:00:00' {n}    --newer 2018-10-27 {n}    --changed-after 1day",
        long = "changed-within",
        alias("change-newer-than", "newer", "changed-after"),
        value_name = "date|dur"
    )]
    pub changed_within: ::std::option::Option<::std::string::String>,
    #[usage(
        help = "Filter by file modification time (older than)",
        long_help = "Filter results based on the file modification time. Files with modification times less than the argument are returned. The argument can be provided as a specific point in time (YYYY-MM-DD HH:MM:SS or @timestamp) or as a duration (10h, 1d, 35min). '--change-older-than' or '--older' can be used as aliases.\n\nExamples: {n}    --changed-before '2018-10-27 10:00:00' {n}    --change-older-than 2weeks {n}    --older 2018-10-27",
        long = "changed-before",
        alias("change-older-than", "older"),
        value_name = "date|dur"
    )]
    pub changed_before: ::std::option::Option<::std::string::String>,
    #[usage(
        help = "Filter by owning user and/or group",
        long_help = "Filter files by their user and/or group. Format: [(user|uid)][:(group|gid)]. Either side is optional. Precede either side with a '!' to exclude files instead.\n\nExamples: {n}    --owner john {n}    --owner :students {n}    --owner '!john:students'",
        long = "owner",
        short = 'o',
        value_name = "user:group"
    )]
    pub owner: ::std::option::Option<::std::string::String>,
    /// Print results according to template
    #[usage(long = "format", conflicts = "--list-details", value_name = "fmt")]
    pub format: ::std::option::Option<::std::string::String>,
    #[usage(
        help = "Execute a command for each search result",
        long_help = "Execute a command for each search result in parallel (use --threads=1 for sequential command execution). There is no guarantee of the order commands are executed in, and the order should not be depended upon. All positional arguments following --exec are considered to be arguments to the command - not to fd. It is therefore recommended to place the '-x'/'--exec' option last. Use '\\;' to terminate the command template if you need to continue passing fd arguments afterwards.\nThe following placeholders are substituted before the command is executed:\n  '{}':   path (of the current search result)\n  '{/}':  basename\n  '{//}': parent directory\n  '{.}':  path without file extension\n  '{/.}': basename without file extension\n  '{{':   literal '{' (for escaping)\n  '}}':   literal '}' (for escaping)\n\nIf no placeholder is present, an implicit \"{}\" at the end is assumed.\n\nExamples:\n\n  - find all *.zip files and unzip them:\n\n      fd -e zip -x unzip\n\n  - find *.h and *.cpp files and run \"clang-format -i ..\" for each of them:\n\n      fd -e h -e cpp -x clang-format -i\n\n  - search within `src/` and echo each match (place `-x` last):\n\n      fd . src -x echo\n\n  - Convert all *.jpg files to *.png files:\n\n      fd -e jpg -x convert {} {.}.png",
        long = "exec",
        short = 'x',
        group = "execs",
        conflicts = "--list-details",
        value_terminator = ";",
        allow_hyphen_values,
        value_name = "cmd",
        var,
        variadic,
        var_min = 1
    )]
    pub exec: ::std::vec::Vec<::std::string::String>,
    #[usage(
        help = "Execute a command with all search results at once",
        long_help = "Execute the given command once, with all search results as arguments.\nThe order of the arguments is non-deterministic, and should not be relied upon.\nOne of the following placeholders is substituted before the command is executed:\n  '{}':   path (of all search results)\n  '{/}':  basename\n  '{//}': parent directory\n  '{.}':  path without file extension\n  '{/.}': basename without file extension\n  '{{':   literal '{' (for escaping)\n  '}}':   literal '}' (for escaping)\n\nIf no placeholder is present, an implicit \"{}\" at the end is assumed.\n\nExamples:\n\n  - Find all test_*.py files and open them in your favorite editor:\n\n      fd -g 'test_*.py' -X vim\n\n  - Find all *.rs files and count the lines with \"wc -l ...\":\n\n      fd -e rs -X wc -l",
        long = "exec-batch",
        short = 'X',
        group = "execs",
        conflicts("--exec", "--list-details"),
        value_terminator = ";",
        allow_hyphen_values,
        value_name = "cmd",
        var,
        variadic,
        var_min = 1
    )]
    pub exec_batch: ::std::vec::Vec<::std::string::String>,
    #[usage(
        help = "Max number of arguments to run as a batch size with -X",
        long_help = "Maximum number of arguments to pass to the command given with -X. If the number of results is greater than the given size, the command given with -X is run again with remaining arguments. A batch size of zero means there is no limit (default), but note that batching might still happen due to OS restrictions on the maximum length of command lines.",
        long = "batch-size",
        value_name = "size",
        default = "0"
    )]
    pub batch_size: ::std::option::Option<::std::string::String>,
    #[usage(
        help = "Add a custom ignore-file in '.gitignore' format",
        long_help = "Add a custom ignore-file in '.gitignore' format. These files have a low precedence.",
        long = "ignore-file",
        value_name = "path",
        var,
        variadic
    )]
    pub ignore_file: ::std::vec::Vec<::std::string::String>,
    #[usage(
        help = "When to use colors",
        long_help = "Declare when to use color for the pattern match output",
        long = "color",
        short = 'c',
        value_name = "when",
        value_enum,
        default = "auto"
    )]
    pub color: ::std::option::Option<ColorValue>,
    #[usage(
        help = "Add hyperlinks to output paths",
        long_help = "Add a terminal hyperlink to a file:// url for each path in the output.\n\nAuto mode  is used if no argument is given to this option.\n\nThis doesn't do anything for --exec and --exec-batch.",
        long = "hyperlink",
        alias = "hyper",
        require_equals,
        value_optional,
        value_name = "when",
        value_enum,
        default = "never"
    )]
    pub hyperlink: ::std::option::Option<::std::option::Option<HyperlinkValue>>,
    /// Ignore directories containing the named entry
    #[usage(long = "ignore-contain", value_name = "name", var, variadic)]
    pub ignore_contain: ::std::vec::Vec<::std::string::String>,
    /// Set number of threads to use for searching & executing (default: number of available CPU cores)
    #[usage(long = "threads", short = 'j', value_name = "num")]
    pub threads: ::std::option::Option<::std::string::String>,
    /// Milliseconds to buffer before streaming search results to console
    ///
    /// Amount of time in milliseconds to buffer, before streaming the search results to the console.
    #[usage(long = "max-buffer-time", hide, value_name = "MAX_BUFFER_TIME")]
    pub max_buffer_time: ::std::option::Option<::std::string::String>,
    #[usage(
        help = "Limit the number of search results",
        long_help = "Limit the number of search results to 'count' and quit immediately.",
        long = "max-results",
        value_name = "count"
    )]
    pub max_results: ::std::option::Option<::std::string::String>,
    #[usage(
        help = "Limit search to a single result",
        long_help = "Limit the search to a single result and quit immediately. This is an alias for '--max-results=1'.",
        short = '1'
    )]
    pub v1: bool,
    #[usage(
        help = "Print nothing, exit code 0 if match found, 1 otherwise",
        long_help = "When the flag is present, the program does not print anything and will return with an exit code of 0 if there is at least one match. Otherwise, the exit code will be 1. '--has-results' can be used as an alias.",
        long = "quiet",
        alias = "has-results",
        short = 'q',
        conflicts = "--max-results"
    )]
    pub quiet: bool,
    #[usage(
        help = "Show filesystem errors",
        long_help = "Enable the display of filesystem errors for situations such as insufficient permissions or dead symlinks.",
        long = "show-errors"
    )]
    pub show_errors: bool,
    #[usage(
        help = "Change current working directory",
        long_help = "Change the current working directory of fd to the provided path. This means that search results will be shown with respect to the given base path. Note that relative paths which are passed to fd via the positional <path> argument or the '--search-path' option will also be resolved relative to this directory.",
        long = "base-directory",
        short = 'C',
        value_name = "path"
    )]
    pub base_directory: ::std::option::Option<::std::string::String>,
    #[usage(
        help = "Set path separator when printing file paths",
        long_help = "Set the path separator to use when printing file paths. The default is the OS-specific separator ('/' on Unix, '\\' on Windows).",
        long = "path-separator",
        value_name = "separator"
    )]
    pub path_separator: ::std::option::Option<::std::string::String>,
    #[usage(
        help = "Provides paths to search as an alternative to the positional <path> argument",
        long_help = "Provide paths to search as an alternative to the positional <path> argument. Changes the usage to `fd [OPTIONS] --search-path <path> --search-path <path2> [<pattern>]`",
        long = "search-path",
        conflicts = "path",
        var,
        variadic
    )]
    pub search_path: ::std::vec::Vec<::std::string::String>,
    #[usage(
        help = "By default, relative paths are prefixed with './' when -x/--exec, -X/--exec-batch, or -0/--print0 are given, to reduce the risk of a path starting with '-' being treated as a command line option. Use this flag to change this behavior. If this flag is used without a value, it is equivalent to passing \"always\"",
        long_help = "By default, relative paths are prefixed with './' when -x/--exec, -X/--exec-batch, or -0/--print0 are given, to reduce the risk of a path starting with '-' being treated as a command line option. Use this flag to change this behavior. If this flag is used without a value, it is equivalent to passing \"always\".",
        long = "strip-cwd-prefix",
        conflicts("path", "--search-path"),
        require_equals,
        value_optional,
        value_name = "when",
        value_enum
    )]
    pub strip_cwd_prefix: ::std::option::Option<::std::option::Option<StripCwdPrefixValue>>,
    #[usage(
        help = "By default, fd will traverse the file system tree as far as other options dictate. With this flag, fd ensures that it does not descend into a different file system than the one it started in. Comparable to the -mount or -xdev filters of find(1)",
        long_help = "By default, fd will traverse the file system tree as far as other options dictate. With this flag, fd ensures that it does not descend into a different file system than the one it started in. Comparable to the -mount or -xdev filters of find(1).",
        long = "one-file-system",
        alias("mount", "xdev")
    )]
    pub one_file_system: bool,
    #[usage(
        long = "gen-completions",
        hide,
        exclusive = true,
        value_optional,
        value_name = "GEN_COMPLETIONS",
        choices("bash", "elvish", "fish", "powershell", "zsh")
    )]
    pub gen_completions: ::std::option::Option<::std::option::Option<::std::string::String>>,
    #[usage(
        arg,
        name = "pattern",
        help = "the search pattern (a regular expression, unless '--glob' is used; optional)",
        long_help = "the search pattern which is either a regular expression (default) or a glob pattern (if --glob is used). If no pattern has been specified, every entry is considered a match. If your pattern starts with a dash (-), make sure to pass '--' first, or it will be considered as a flag (fd -- '-foo').",
        default = ""
    )]
    pub pattern: ::std::option::Option<::std::string::String>,
    #[usage(
        arg,
        name = "path",
        help = "the root directories for the filesystem search (optional)",
        long_help = "The directory where the filesystem search is rooted (optional). If omitted, search the current working directory.",
        var_min = 1
    )]
    pub path: ::std::vec::Vec<::std::string::String>,
}

#[derive(ValueEnum)]
pub enum TypeValue {
    #[value(name = "file", alias = "f")]
    File,
    #[value(name = "directory", alias = "d", alias = "dir")]
    Directory,
    #[value(name = "symlink", alias = "l")]
    Symlink,
    #[value(name = "block-device", alias = "b")]
    BlockDevice,
    #[value(name = "char-device", alias = "c")]
    CharDevice,
    /// A file which is executable by the current effective user
    #[value(name = "executable", alias = "x")]
    Executable,
    #[value(name = "empty", alias = "e")]
    Empty,
    #[value(name = "socket", alias = "s")]
    Socket,
    #[value(name = "pipe", alias = "p")]
    Pipe,
}

#[derive(ValueEnum)]
pub enum ColorValue {
    /// show colors if the output goes to an interactive console (default)
    #[value(name = "auto")]
    Auto,
    /// always use colorized output
    #[value(name = "always")]
    Always,
    /// do not use colorized output
    #[value(name = "never")]
    Never,
}

#[derive(ValueEnum)]
pub enum HyperlinkValue {
    /// Use hyperlinks only if color is enabled
    #[value(name = "auto")]
    Auto,
    /// Always use hyperlinks when printing file paths
    #[value(name = "always")]
    Always,
    /// Never use hyperlinks
    #[value(name = "never")]
    Never,
}

#[derive(ValueEnum)]
pub enum StripCwdPrefixValue {
    /// Use the default behavior
    #[value(name = "auto")]
    Auto,
    /// Always strip the ./ at the beginning of paths
    #[value(name = "always")]
    Always,
    /// Never strip the ./
    #[value(name = "never")]
    Never,
}
