---
source: lib/src/sdk/rust/mod.rs
expression: client
---
//! @generated by usage-cli from test.usage.kdl. Do not edit manually.

use crate::runtime::{CliRunner, CliResult, CliError};
use crate::types::*;

#[derive(Debug, Clone)]
pub struct App {
    runner: CliRunner,
    pub status: Status,
    pub info: Info,
}

impl App {
    pub fn new(bin_path: &str) -> Self {
        Self::with_runner(CliRunner::new(bin_path))
    }

    pub fn with_runner(runner: CliRunner) -> Self {
        Self {
            runner: runner.clone(),
            status: Status::new(runner.clone()),
            info: Info::new(runner.clone()),
        }
    }

    /// [-v --verbose] <SUBCOMMAND>
    pub fn exec(&self, flags: Option<&AppFlags>) -> Result<CliResult, CliError> {
        let mut cmd_args: Vec<String> = vec![];
        cmd_args.extend(Self::build_flag_args(flags));
        self.runner.run(cmd_args)
    }

    fn build_flag_args(flags: Option<&AppFlags>) -> Vec<String> {
        let mut result = Vec::new();
        let Some(flags) = flags else { return result };
        if flags.verbose == Some(true) {
            result.push("--verbose".to_string());
        }
        result
    }
}

/// Show status
#[derive(Debug, Clone)]
pub struct Status {
    runner: CliRunner,
}

impl Status {
    pub(crate) fn new(runner: CliRunner) -> Self {
        Self {
            runner: runner.clone(),
        }
    }

    /// status [--json]
    pub fn exec(&self, flags: Option<&StatusFlags>) -> Result<CliResult, CliError> {
        let mut cmd_args: Vec<String> = vec!["status".to_string()];
        cmd_args.extend(Self::build_flag_args(flags));
        self.runner.run(cmd_args)
    }

    fn build_flag_args(flags: Option<&StatusFlags>) -> Vec<String> {
        let mut result = Vec::new();
        let Some(flags) = flags else { return result };
        if flags.verbose == Some(true) {
            result.push("--verbose".to_string());
        }
        if flags.json == Some(true) {
            result.push("--json".to_string());
        }
        result
    }
}

/// Show info
#[derive(Debug, Clone)]
pub struct Info {
    runner: CliRunner,
}

impl Info {
    pub(crate) fn new(runner: CliRunner) -> Self {
        Self {
            runner: runner.clone(),
        }
    }

    /// info
    pub fn exec(&self, flags: Option<&GlobalFlags>) -> Result<CliResult, CliError> {
        let mut cmd_args: Vec<String> = vec!["info".to_string()];
        cmd_args.extend(Self::build_flag_args(flags));
        self.runner.run(cmd_args)
    }

    fn build_flag_args(flags: Option<&GlobalFlags>) -> Vec<String> {
        let mut result = Vec::new();
        let Some(flags) = flags else { return result };
        if flags.verbose == Some(true) {
            result.push("--verbose".to_string());
        }
        result
    }
}
